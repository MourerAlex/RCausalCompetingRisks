% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{boot_arm_matrices}
\alias{boot_arm_matrices}
\title{Per-arm bootstrap matrices for one method}
\usage{
boot_arm_matrices(replicates, method, arms = NULL)
}
\description{
Pivots the long \code{replicates} table (filtered to \code{method}) into a named
list of \verb{[n_boot x n_times]} matrices, one per arm — rows are bootstrap
replicates (sorted by \code{boot_id}), columns are cut times (sorted by \code{k}).
Mirrors the old squeezed array slice \code{boot_array[, method, arm, ]}. Arms
absent from the method get an all-\code{NA} matrix so downstream cross-arm
algebra keeps its shape (and yields \code{NA} CIs, matching the \code{NA} point
estimates for unsupported decompositions).
}
\keyword{internal}
