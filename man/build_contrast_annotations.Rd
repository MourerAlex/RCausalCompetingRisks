% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{build_contrast_annotations}
\alias{build_contrast_annotations}
\title{Build Contrast Annotation Rows for plot.causal_competing_risks_risk}
\usage{
build_contrast_annotations(
  risk_method,
  have_bands,
  boot_method,
  contrast_annotations,
  eval_times,
  alpha_val = NULL
)
}
\arguments{
\item{boot_method}{Named list of per-arm \verb{[n_boot x n_times]} replicate
matrices for the chosen method (from \code{\link[=boot_arm_matrices]{boot_arm_matrices()}}), or NULL
if no bootstrap.}

\item{contrast_annotations}{Character vector (validated upstream).}

\item{eval_times}{Numeric vector of time points (user-specified, may
not match \code{cut_times} exactly).}

\item{alpha_val}{Numeric in (0, 1) or NULL. Significance level for the
contrast CI (from the bootstrap object). NULL means no CI in label.}

\item{ci_df}{Data.frame with \code{k} and the arm columns (point estimates).}

\item{ci_bands}{Data.frame with \code{k} and \verb{\{arm\}_lower} / \verb{\{arm\}_upper}
columns, or NULL if no bootstrap was supplied.}
}
\value{
Data.frame with columns \code{k}, \code{annotation}, \code{y_low}, \code{y_high},
\code{y_label}, \code{label}.
}
\description{
For each (annotation, eval_time) pair, compute the bridge endpoints
(y_low, y_high) from the two arms in the contrast, plus a numeric label
showing the point estimate. If bootstrap replicates (\code{boot_method}) are
supplied, computes a PROPER per-contrast percentile CI by taking
per-replicate differences then quantiles (NOT the naive difference of
per-arm CIs, which is too wide because it ignores cross-arm covariance
within each bootstrap replicate).
}
\details{
Also computes \code{y_label} — the y-coordinate where the label should be
drawn. This sits below the lower CI band at that time point (or below
the lower arm curve if no CI bands), slightly offset for legibility.

Emits a one-time \code{message()} if any \code{eval_times} had to be snapped to the
nearest \code{cut_times} (when they didn't match exactly).
}
\seealso{
Other internal:
\code{\link[=arm_spec]{arm_spec()}},
\code{\link[=build_risk_long]{build_risk_long()}},
\code{\link[=build_risk_table_plot]{build_risk_table_plot()}},
\code{\link[=compute_contrasts]{compute_contrasts()}},
\code{\link[=compute_cum_inc]{compute_cum_inc()}},
\code{\link[=fit_gformula]{fit_gformula()}},
\code{\link[=fit_hazard_models]{fit_hazard_models()}},
\code{\link[=fit_separable_effects]{fit_separable_effects()}},
\code{\link[=isolation_summary]{isolation_summary()}},
\code{\link[=make_clone]{make_clone()}},
\code{\link[=predict_hazards]{predict_hazards()}},
\code{\link[=risk_table_internal]{risk_table_internal()}},
\code{\link[=validate_person_time]{validate_person_time()}},
\code{\link[=validate_subject_level]{validate_subject_level()}}
}
\concept{internal}
\keyword{internal}
