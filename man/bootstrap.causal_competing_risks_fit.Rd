% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap.R
\name{bootstrap.causal_competing_risks_fit}
\alias{bootstrap.causal_competing_risks_fit}
\title{Bootstrap Confidence Intervals for a causal_competing_risks Fit}
\usage{
\method{bootstrap}{causal_competing_risks_fit}(fit, n_boot = 500, alpha = 0.05, seed = NULL, verbose = TRUE, ...)
}
\arguments{
\item{fit}{A \code{"causal_competing_risks_fit"} object from \code{\link[=causal_competing_risks]{causal_competing_risks()}}.}

\item{n_boot}{Integer. Number of bootstrap replicates (default 500).}

\item{alpha}{Numeric. Two-sided significance level (default 0.05 for
95\% CIs).}

\item{seed}{Optional integer RNG seed for reproducibility (set once
before the replicate loop).}

\item{verbose}{Logical. Print progress during the replicate loop
(default \code{TRUE}).}

\item{...}{Additional arguments (currently unused).}
}
\value{
An S3 object of class \code{"causal_competing_risks_bootstrap"}:
\describe{
\item{replicates}{Long data.frame \verb{(boot_id, method, arm, k, value)}
of cumulative incidence estimates per bootstrap draw.}
\item{bands}{Long data.frame \verb{(method, arm, k, lower, upper)}: the
\code{alpha/2} and \code{1 - alpha/2} percentile bands per (method, arm, k).}
\item{n_boot_requested, n_boot_effective}{Replicates requested and
the number that survived (failures dropped).}
\item{alpha}{The significance level used.}
\item{failed_reps}{Integer indices of replicates whose refit errored.}
\item{fit_call}{Copy of \code{fit$call} for provenance.}
}
}
\description{
Performs subject-level bootstrap resampling on a \code{\link[=causal_competing_risks]{causal_competing_risks()}} fit and
constructs percentile confidence intervals for cumulative incidence
curves. Returns a separate \code{"causal_competing_risks_bootstrap"} object that pairs
with the fit — pass both to plotting and contrast functions to get
confidence bands.
}
\details{
\subsection{Resampling scheme}{

Subject-level resampling with replacement. For each replicate, unique
IDs are sampled with replacement; the person-time rows for each sampled
ID are pulled (via a pre-split lookup). Duplicate draws are given unique
synthetic IDs so each resampled subject is treated as distinct.
}

\subsection{Estimation per replicate}{

Calls \code{\link[=fit_separable_effects]{fit_separable_effects()}} directly (not \code{\link[=causal_competing_risks]{causal_competing_risks()}}), bypassing the
user-facing wrapper's validation and warning capture. Warnings during
replicates are suppressed (they would otherwise spam).
}

\subsection{CI construction}{

Percentile method: \code{alpha/2} and \code{1 - alpha/2} quantiles of the bootstrap
distribution at each time point, computed per arm per method.
}

\subsection{Progress reporting}{

For n_boot > 50: prints every 10 replicates for the first 50, then
prints a time estimate for the remaining replicates (based on the first
50), then every 100 after. For n_boot <= 50: prints every 10.
}
}
\examples{
\dontrun{
fit <- causal_competing_risks(pt)
boot <- bootstrap(fit, n_boot = 500)
plot(risk(fit), ci = boot)
contrast(fit, method = "gformula", ci = boot)
}

}
\seealso{
\code{\link[=causal_competing_risks]{causal_competing_risks()}}, \code{\link[=fit_separable_effects]{fit_separable_effects()}}, \code{\link[=contrast]{contrast()}}, \code{\link[=risk]{risk()}}
}
