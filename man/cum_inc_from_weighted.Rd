% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competing_cif.R
\name{cum_inc_from_weighted}
\alias{cum_inc_from_weighted}
\title{Cumulative Incidence from Weighted Person-Time Data}
\usage{
cum_inc_from_weighted(y_event, d_event, c_event, k, weights, cut_times)
}
\arguments{
\item{y_event, d_event, c_event}{Numeric vectors of event indicators
(Y, D, censoring), no-NA \verb{\{0, 1\}} schema.}

\item{k}{Vector of interval indices.}

\item{weights}{Numeric vector of per-row weights.}

\item{cut_times}{Numeric vector of time points to evaluate at.}
}
\value{
Numeric vector of cumulative incidence at each \code{cut_times[k]}.
}
\description{
Discrete-time cumulative incidence of \code{Y} in the presence of competing event
\code{D}, computed from per-row event indicators and Hajek-weighted hazards. The
caller is responsible for restricting inputs to a single standing-in arm and
for building the per-row \code{weights} (IPTW * IPCW * separable swap); it
is weight-agnostic — it only applies whatever weights it is given.
}
\details{
Forms weighted hazards by interval via \code{\link[CausalSurvival:weighted_hazard_by_k]{weighted_hazard_by_k()}}, aligns them
to \code{cut_times}, and delegates the discrete-time recursion to
\code{\link[=cumulative_incidence]{cumulative_incidence()}}.
}
\keyword{internal}
