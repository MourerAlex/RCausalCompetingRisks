% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gformula.R
\name{fit_gformula}
\alias{fit_gformula}
\title{G-Formula Estimation of Separable Effects}
\usage{
fit_gformula(pt_data, models, cut_times, id_col)
}
\arguments{
\item{pt_data}{Data frame in person-time format. Every subject must have
a row at \code{k = 0} (enforced by \code{\link[=validate_person_time]{validate_person_time()}}).}

\item{models}{List of fitted glm objects from \code{\link[CausalSurvival:fit_hazard_models]{fit_hazard_models()}}.}

\item{cut_times}{Numeric vector. Time cut points.}

\item{id_col}{Character. Subject identifier column name.}
}
\value{
A data.frame with columns \code{k}, \code{arm_11}, \code{arm_00}, \code{arm_10},
\code{arm_01}. The last enables Decomposition B sensitivity (see Details).
}
\description{
Estimates cumulative incidence under each treatment arm using the
parametric g-formula. Creates cloned datasets for arms (1,1), (0,0),
(1,0), and (0,1), predicts hazards using the cross-arm trick, and
computes per-subject cumulative incidence averaged across subjects.
}
\details{
\subsection{The cross-arm prediction trick}{

For arm \verb{(a_Y, a_D)}:
\itemize{
\item Predict Y-hazard using \code{model_y} with \code{A_y = a_Y}
\item Predict D-hazard using \code{model_d} with \code{A_d = a_D}
}

Arms \verb{(1,1)} and \verb{(0,0)} use the same treatment value in both models.
Arms \verb{(1,0)} and \verb{(0,1)} are the cross-arm regimes.
}

\subsection{Decomposition A vs B}{
\itemize{
\item Decomposition A: direct = \code{arm_10 - arm_00}, indirect = \code{arm_11 - arm_10}
(reference: \code{a_D = 0} in the direct)
\item Decomposition B: direct = \code{arm_11 - arm_01}, indirect = \code{arm_01 - arm_00}
(reference: \code{a_D = 1} in the direct)
}

Both sum to the same total effect \code{arm_11 - arm_00}. They differ whenever
the \code{A_Y} effect on \code{Y} depends on \code{A_D} (additive \verb{A_Y x A_D} interaction).
}

\subsection{Cumulative incidence}{

Per-subject computation, then averaged:
\deqn{F_Y(t) = \frac{1}{n} \sum_i \sum_{s \leq t}
  h_Y(s|i) (1 - h_D(s|i)) S(s-1|i)}

where \eqn{S(s|i) = \prod_{r \leq s} (1 - h_Y(r|i))(1 - h_D(r|i))}.

The cumprod is computed per-subject first (it does not commute with the
mean). The code order (mean by k, then cumsum) is mathematically equivalent
to (cumsum per subject, then mean) by commutativity of the double sum,
and is more memory-efficient.
}

\subsection{Baseline extraction}{

Uses \code{pt_data$k == 1} directly (baseline interval under the CausalSurvival
\verb{(0, t_1], ..., (t_\{K_max-1\}, T_max]} grid, where \code{k} is the integer
interval index starting at 1). Left-truncated data (subjects with no
\code{k = 1} row) is not supported in any future version.
}
}
\seealso{
Other internal:
\code{\link[=arm_spec]{arm_spec()}},
\code{\link[=build_contrast_annotations]{build_contrast_annotations()}},
\code{\link[=build_risk_long]{build_risk_long()}},
\code{\link[=build_risk_table_plot]{build_risk_table_plot()}},
\code{\link[=compute_contrasts]{compute_contrasts()}},
\code{\link[=compute_cum_inc]{compute_cum_inc()}},
\code{\link[=fit_hazard_models]{fit_hazard_models()}},
\code{\link[=fit_separable_effects]{fit_separable_effects()}},
\code{\link[=isolation_summary]{isolation_summary()}},
\code{\link[=make_clone]{make_clone()}},
\code{\link[=predict_hazards]{predict_hazards()}},
\code{\link[=risk_table_internal]{risk_table_internal()}},
\code{\link[=validate_person_time]{validate_person_time()}},
\code{\link[=validate_subject_level]{validate_subject_level()}}
}
\concept{internal}
\keyword{internal}
