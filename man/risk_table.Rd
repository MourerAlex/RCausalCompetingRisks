% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_table.R
\name{risk_table}
\alias{risk_table}
\title{Compute Risk Table Counts by Observed Treatment Arm}
\usage{
risk_table(fit, count = c("at_risk", "events_y", "events_d", "censored"))
}
\arguments{
\item{fit}{A \code{"causal_competing_risks_fit"} object from \code{\link[=causal_competing_risks]{causal_competing_risks()}}.}

\item{count}{Character. One of:
\describe{
\item{\code{"at_risk"}}{Number of subjects with a row at k (still under
observation at the start of interval k). A snapshot, not cumulative.}
\item{\code{"events_y"}}{\strong{Cumulative} number of primary (Y) events through
interval k.}
\item{\code{"events_d"}}{\strong{Cumulative} number of competing (D) events
through interval k.}
\item{\code{"censored"}}{\strong{Cumulative} number of subjects censored through
interval k.}
}}
}
\value{
A data.frame with:
\describe{
\item{k}{Time point (from \code{fit$times}).}
\item{\verb{<treatment>_<value>}}{One column per observed treatment level,
named e.g. \code{A_0} and \code{A_1}.}
}
}
\description{
Returns counts at each \code{cut_times} value, grouped by observed treatment
group (e.g., A = 0 and A = 1). Complements cumulative incidence curves
for reporting (adjustedCurves-style risk tables).
}
\details{
The user picks \strong{one} count type per call. Counts are computed on
observed subjects — NOT on counterfactual arms (\code{arm_10}, \code{arm_01}
would not correspond to any real subjects).
}
\examples{
\dontrun{
fit <- causal_competing_risks(pt)
risk_table(fit, count = "at_risk")
risk_table(fit, count = "events_y")
}

}
\seealso{
\code{\link[=causal_competing_risks]{causal_competing_risks()}}, \code{\link[=plot.causal_competing_risks_risk]{plot.causal_competing_risks_risk()}}

Other accessors:
\code{\link[=assumptions]{assumptions()}},
\code{\link[=contrast]{contrast()}},
\code{\link[=diagnostic]{diagnostic()}},
\code{\link[=risk]{risk()}}
}
\concept{accessors}
