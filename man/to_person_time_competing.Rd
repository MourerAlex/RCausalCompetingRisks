% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{to_person_time_competing}
\alias{to_person_time_competing}
\title{Prepare Subject-Level Competing-Events Data for \code{\link[=causal_competing_risks]{causal_competing_risks()}}}
\usage{
to_person_time_competing(
  data,
  id = "id",
  time = "event_time",
  event = "event_type",
  treatment = "A",
  covariates = character(),
  event_y,
  event_d,
  event_c,
  ipcw = TRUE,
  T_max = NULL,
  cut_points = NULL,
  time_varying = NULL,
  ...
)
}
\arguments{
\item{data}{Subject-level data.frame (one row per subject).}

\item{id, time, treatment, covariates}{See \code{\link[CausalSurvival:to_person_time]{CausalSurvival::to_person_time()}}.}

\item{event}{Character. Name of the multi-valued event-type column.}

\item{event_y, event_d, event_c}{Values in \code{event} marking the primary
event (Y), the competing event (D), and censoring, respectively.}

\item{ipcw, T_max, cut_points, time_varying}{See
\code{\link[CausalSurvival:to_person_time]{CausalSurvival::to_person_time()}}. \code{cut_points} follows the CS
convention: \code{NULL} = 12 equi-spaced intervals; a positive integer =
that many intervals; a vector = interior cut points within \verb{(0, T_max)}.}

\item{...}{Unused. Reserved to catch the renamed \code{n_intervals} argument
with a clear error.}
}
\value{
A \code{c("person_time", "data.frame")} carrying CS's columns plus
\code{d_event}, a single collapsed \code{c_event} (CS's \code{cond_indep_cens} /
\code{indep_cens} merged), \code{A_y}, \code{A_d}, and an \code{event_labels} attribute
\code{list(y, d, c)}.
Per row at most one of \verb{y_event, d_event, c_event} is 1.
}
\description{
Discretizes subject-level competing-events data (one row per subject,
a single multi-valued \code{event} column) into person-time format. This is
a thin wrapper over \code{\link[CausalSurvival:to_person_time]{CausalSurvival::to_person_time()}}: the generic
single-event machinery (the \verb{(k-1, k]} grid, \code{T_max} administrative
truncation, three-way censoring split, treatment standardization) is
inherited, and the competing event D is layered on top.
}
\details{
The competing event is passed through the single-event engine as an
event (not as censoring) so that a D landing exactly at \code{T_max} is not
lost to admin-truncation, then recovered into its own \code{d_event} flag.
}
\seealso{
\code{\link[CausalSurvival:to_person_time]{CausalSurvival::to_person_time()}}, \code{\link[=causal_competing_risks]{causal_competing_risks()}}
}
