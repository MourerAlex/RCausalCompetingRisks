% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrasts.R
\name{compute_contrasts}
\alias{compute_contrasts}
\title{Compute Causal Contrasts (Long Format with Confidence Intervals)}
\usage{
compute_contrasts(fit, method, ci)
}
\arguments{
\item{fit}{A \code{"causal_competing_risks_fit"} object.}

\item{method}{Character (length 1). Which method to extract from
\code{fit$cumulative_incidence} and \code{ci$replicates}. One of the names in
\code{fit$active_methods}.}

\item{ci}{A \code{"causal_competing_risks_bootstrap"} object from \code{\link[CausalSurvival:bootstrap]{bootstrap()}}.}
}
\value{
A long-format data.frame with columns:
\describe{
\item{k}{Time point.}
\item{contrast}{One of \code{"total"}, \code{"sep_direct"}, \code{"sep_indirect"}.}
\item{decomp}{\code{NA} for totals, \code{"A"} or \code{"B"} for direct/indirect.}
\item{measure}{\code{"rd"} or \code{"rr"}.}
\item{estimate}{Point estimate.}
\item{lower,upper}{Percentile confidence interval bounds from the
bootstrap distribution (\code{alpha/2} and \code{1 - alpha/2}).}
}

Rows per time point: 10 (1 total + 2 direct + 2 indirect, each for \code{rd}
and \code{rr}). For methods that do not emit \code{arm_01} (IPW Rep 1 and Rep 2),
Decomposition B rows have \code{NA} estimates and CIs.
}
\description{
Builds the long-format contrast table for a single estimation method by
combining the method's point estimates (from the fit) with bootstrap
quantiles (from a \code{"causal_competing_risks_bootstrap"} object). Used internally by
\code{\link[=contrast]{contrast()}}.
}
\details{
Both decompositions share the same reference arm (\code{arm_00}) and the same
total (\code{arm_11 - arm_00}). They differ only in the \strong{intermediate arm} —
the path order from \code{arm_00} to \code{arm_11}. Convention: arm \code{"xy"} means
\verb{(a_Y = x, a_D = y)}. \code{A_Y} acts on the path A → Y not through D (its
effect is the \emph{direct} effect, SDE). \code{A_D} acts on the path A → D → Y
(its effect is the \emph{indirect} effect, SIE).
\subsection{Decomposition A — vary \code{A_Y} first, then \code{A_D} (intermediate arm \code{arm_10})}{
\itemize{
\item \code{direct  (SDE-A) = arm_10 - arm_00}  (effect of \code{A_Y} at \code{a_D = 0})
\item \code{indirect (SIE-A) = arm_11 - arm_10} (effect of \code{A_D} at \code{a_Y = 1})
}
}

\subsection{Decomposition B — vary \code{A_D} first, then \code{A_Y} (intermediate arm \code{arm_01})}{
\itemize{
\item \code{indirect (SIE-B) = arm_01 - arm_00} (effect of \code{A_D} at \code{a_Y = 0})
\item \code{direct  (SDE-B) = arm_11 - arm_01}  (effect of \code{A_Y} at \code{a_D = 1})
}

Total = \code{arm_11 - arm_00} is identical under both decompositions, and the
algebraic identity \code{SDE + SIE = Total} holds for each.
}
}
\references{
Stensrud MJ, Young JG, Didelez V, Robins JM, Hernán MA (2020).
Separable effects for causal inference in the presence of competing events.
\emph{Journal of the American Statistical Association}, §3.
\doi{10.1080/01621459.2020.1765783}
}
\seealso{
Other internal:
\code{\link[=arm_spec]{arm_spec()}},
\code{\link[=build_contrast_annotations]{build_contrast_annotations()}},
\code{\link[=build_risk_long]{build_risk_long()}},
\code{\link[=build_risk_table_plot]{build_risk_table_plot()}},
\code{\link[=compute_cum_inc]{compute_cum_inc()}},
\code{\link[=fit_gformula]{fit_gformula()}},
\code{\link[=fit_hazard_models]{fit_hazard_models()}},
\code{\link[=fit_separable_effects]{fit_separable_effects()}},
\code{\link[=isolation_summary]{isolation_summary()}},
\code{\link[=make_clone]{make_clone()}},
\code{\link[=predict_hazards]{predict_hazards()}},
\code{\link[=risk_table_internal]{risk_table_internal()}},
\code{\link[=validate_person_time]{validate_person_time()}},
\code{\link[=validate_subject_level]{validate_subject_level()}}
}
\concept{internal}
\keyword{internal}
