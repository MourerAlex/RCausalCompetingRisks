% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hazards.R
\name{fit_hazard_models}
\alias{fit_hazard_models}
\title{Fit Hazard Models}
\usage{
fit_hazard_models(
  pt_data,
  treatment,
  covariates,
  active_methods,
  formulas,
  ipcw = TRUE
)
}
\arguments{
\item{pt_data}{Data frame in person-time format.}

\item{treatment}{Character. Treatment column name (used for the censoring
model; Y and D models use \code{A_y} / \code{A_d} working copies).}

\item{covariates}{Character vector. Covariate column names.}

\item{active_methods}{Character vector. Subset of
\code{c("gformula", "ipw_rep1", "ipw_rep2")} indicating which methods will run.
Determines which models get fit.}

\item{formulas}{Named list or NULL. User-specified formulas (names \code{y},
\code{d}, \code{c}). Any entry absent falls back to the default formula.}

\item{ipcw}{Logical. When FALSE, the censoring model is not
fit and \code{model_c} stays NULL.}
}
\value{
A named list with two entries:
\describe{
\item{models}{Named list: \code{model_y}, \code{model_d}, \code{model_c} (glm
objects or NULL).}
\item{checks}{Named list: \code{y}, \code{d}, \code{c} (per-model diagnostics or
NULL). See \code{\link[CausalSurvival:check_fitted_positivity]{check_fitted_positivity()}} for the per-model structure.}
}
}
\description{
Fits pooled logistic regression models for the Y-hazard, D-hazard, and
(optionally) censoring hazard on person-time data. For each fitted model,
collects diagnostics (convergence, min/max fitted probability, positivity
flag, captured \code{glm()} warnings).
}
\details{
\subsection{Default formula}{

\code{y_event/d_event/c_event ~ A_y/A_d/treatment + k + I(k^2) + I(k^3) + covariates} (additive,
no interaction). The time trend is a cubic polynomial in the \strong{integer
interval index} \code{k} (\verb{1..K_max}), inherited from CausalSurvival's grid.
Because \code{k} is an index, the polynomial measures time on the interval-count
scale, not the clock scale. With equi-spaced cut points the two are an
affine reparameterization (same fit); with \strong{unequally-spaced} cut points
they differ — the trend is then smooth in interval rank, not in elapsed
time. Supply an explicit \code{formulas$y/d/c} using a clock-time term if a
time-scale trend is required.
}

\subsection{Which models per method}{
\itemize{
\item \code{"gformula"}: model_y + model_d
\item \code{"ipw_rep1"}: model_d + model_c
\item \code{"ipw_rep2"}: model_y + model_c
}
}

\subsection{Fit populations (no-NA schema)}{
\itemize{
\item Y- and D-hazard (cause-specific): rows with \code{c_event == 0} (at risk
while uncensored; the competing-event exit row stays in with a 0 on
the other cause).
\item Censoring (\code{c_event}) hazard: all person-time rows.
}
}

\subsection{Treatment handling}{
\itemize{
\item Y-hazard model: uses \code{A_y} (a working-copy column on pt_data)
\item D-hazard model: uses \code{A_d} (same idea)
\item Censoring model: uses the observed \code{treatment} column directly
}

In observed data all three are identical; \code{A_y}/\code{A_d} diverge only in
cloned datasets used for cross-arm prediction downstream.
}
}
\seealso{
Other internal:
\code{\link[=arm_spec]{arm_spec()}},
\code{\link[=build_contrast_annotations]{build_contrast_annotations()}},
\code{\link[=build_risk_long]{build_risk_long()}},
\code{\link[=build_risk_table_plot]{build_risk_table_plot()}},
\code{\link[=compute_contrasts]{compute_contrasts()}},
\code{\link[=compute_cum_inc]{compute_cum_inc()}},
\code{\link[=fit_gformula]{fit_gformula()}},
\code{\link[=fit_separable_effects]{fit_separable_effects()}},
\code{\link[=isolation_summary]{isolation_summary()}},
\code{\link[=make_clone]{make_clone()}},
\code{\link[=predict_hazards]{predict_hazards()}},
\code{\link[=risk_table_internal]{risk_table_internal()}},
\code{\link[=validate_person_time]{validate_person_time()}},
\code{\link[=validate_subject_level]{validate_subject_level()}}
}
\concept{internal}
\keyword{internal}
