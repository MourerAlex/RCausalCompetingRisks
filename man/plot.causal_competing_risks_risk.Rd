% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot.causal_competing_risks_risk}
\alias{plot.causal_competing_risks_risk}
\title{Plot Cumulative Incidence Curves}
\usage{
\method{plot}{causal_competing_risks_risk}(
  x,
  method,
  arms = NULL,
  eval_times = NULL,
  contrast_annotations = NULL,
  risk_table = NULL,
  risk_table_height = 0.23,
  arm_colors = NULL,
  arm_labels = NULL,
  title = NULL,
  subtitle = NULL,
  x_label = "Time (interval k)",
  y_label = "Cumulative incidence",
  base_size = 11,
  annotation_size = 2.8,
  linewidth = 0.8,
  ribbon_alpha = 0.15,
  ...
)
}
\arguments{
\item{x}{A \code{"causal_competing_risks_risk"} object from \code{\link[=risk]{risk()}}.}

\item{method}{Character (length 1). Required. One of
\code{names(x$cumulative_incidence)}. No default.}

\item{arms}{Character vector. Which arms to draw. Defaults to the 3 core
arms (\code{arm_11}, \code{arm_00}, \code{arm_10}); include \code{"arm_01"} explicitly for
Decomposition B sensitivity.}

\item{eval_times}{Numeric vector or NULL. Time points at which to draw
annotations. Only has an effect when \code{contrast_annotations} is also
provided.}

\item{contrast_annotations}{Character vector. Max 2. Which contrasts to
annotate with dotted vertical bridges + numeric labels at \code{eval_times}.
One or two of:
\code{"total"}, \code{"sep_direct_A"}, \code{"sep_indirect_A"}, \code{"sep_direct_B"},
\code{"sep_indirect_B"}.}

\item{risk_table}{NULL (default, no table) or one of \code{"at_risk"},
\code{"events_y"}, \code{"events_d"}, \code{"censored"}. Stacks a counts table below
the curves via \link{/}. The \code{risk()} accessor stores the
needed references — no need to re-pass \code{fit}.}

\item{risk_table_height}{Numeric. Height of the risk table relative to
the main plot (which is 1). Default \code{0.23} (the table is ~23\% of the
main plot's height). Increase to give the table more room.}

\item{arm_colors}{Named character vector overriding arm hex colors.
Defaults to Okabe-Ito. Only the arms you pass are overridden; others
keep the default.}

\item{arm_labels}{Named character vector overriding arm legend labels.}

\item{title}{Character or NULL. Plot title. Default:
\code{"Cumulative incidence (<method>)"}.}

\item{subtitle}{Character or NULL. Plot subtitle. Default: none.}

\item{x_label}{Character. X-axis label. Default \code{"Time (interval k)"}.}

\item{y_label}{Character. Y-axis label. Default \code{"Cumulative incidence"}.}

\item{base_size}{Numeric. Base font size for \code{theme_minimal()}. Scales
all text (title, axes, legend). Default 11.}

\item{annotation_size}{Numeric. Size of contrast annotation label text
(passed to \code{geom_label(size = ...)}, which uses ggplot's mm units).
Default 2.8.}

\item{linewidth}{Numeric. Width of the cumulative incidence step lines.
Default 0.8.}

\item{ribbon_alpha}{Numeric in \link{0, 1}. Transparency of the CI ribbons.
Default 0.15.}

\item{...}{Additional arguments (currently unused).}
}
\value{
A ggplot2 object (or a patchwork object when \code{risk_table} is set).
}
\description{
Plots cumulative incidence for each arm of one estimation method. Pairs
with \code{\link[=risk]{risk()}} — pass a \code{"causal_competing_risks_risk"} object. Optional bootstrap CI
ribbons when the paired \code{risk()} was built with \code{ci = boot}. Optional
contrast annotations (dotted vertical bridges + numeric labels) at
user-specified time points, and an optional risk table stacked below.
}
\details{
Default colors follow the Okabe-Ito colorblind-safe palette:
\itemize{
\item \code{arm_11} (treated)              black    \verb{#000000}
\item \code{arm_00} (control)              blue     \verb{#0072B2}
\item \code{arm_10} (separable, via A_Y=1) green    \verb{#009E73}
\item \code{arm_01} (separable, via A_D=1) vermillion \verb{#D55E00}
}

Override individual entries via \code{arm_colors}. Non-overridden arms keep
their defaults.
}
\concept{plot}
